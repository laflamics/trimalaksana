================================================================================
                    FRESH SETUP - QUICK REFERENCE
================================================================================

LAPTOP DEV (Linux) - DONE ✅
  ✓ Deleted all old data files from data/localStorage/
  ✓ Ready for browser cache clear

PC UTAMA (Windows) - TODO
  1. docker stop docker-storage-server-1
  2. docker rm docker-storage-server-1
  3. docker ps (verify only 4 containers)
  4. del D:\trimalaksanaapps\possgresql\docker\..\data\localStorage\*.json
  
  OR run: cleanup-old-data.bat

BROWSER (Laptop Dev) - TODO
  1. Open DevTools (F12)
  2. Application > Local Storage > Clear All
  3. Reload page (Ctrl+R)

TEST FRESH SYNC - TODO
  1. Settings > Check Connection (should show "Connected")
  2. Packaging > Master > Products > Add new product
  3. Data should PERSIST (not disappear)
  4. Refresh page > data should still be there
  5. Check pgAdmin > data should be in PostgreSQL

================================================================================
                              KEY ENDPOINTS
================================================================================

PC Utama (Windows Server):
  PostgreSQL:    localhost:5432 (user: trimalaksana / pass: trimalaksana123)
  MinIO:         localhost:9000 (user: minioadmin / pass: minioadmin123)
  MinIO Console: http://localhost:9001
  pgAdmin:       http://localhost:5051 (user: admin@trimalaksana.com / pass: admin123)
  Node.js:       http://localhost:8888

Laptop Dev (Linux Client):
  API:           http://10.1.1.35:8888
  WebSocket:     wss://server-tljp.tail75a421.ts.net/ws

================================================================================
                            TROUBLESHOOTING
================================================================================

Data disappears after refresh?
  → Check browser console (F12 > Console) for errors
  → Check server logs: docker-compose -f docker-compose-migration.yml logs storage-server
  → Verify WebSocket: DevTools > Network > WS tab

Connection fails?
  → Verify PC Utama IP: 10.1.1.35
  → Verify 4 containers running: docker ps
  → Check firewall allows port 8888

PostgreSQL not accessible?
  → Verify pgAdmin port is 5051 (not 5050)
  → Check pgAdmin logs: docker-compose -f docker-compose-migration.yml logs pgadmin

================================================================================
                              CLEANUP SCRIPTS
================================================================================

PC Utama:
  docker/cleanup-old-container.bat  - Remove old docker container
  docker/cleanup-old-data.bat       - Delete old data files

Laptop Dev:
  cleanup-old-data.sh               - Delete old data files

================================================================================
